spring:
  application:
    name: Mech_App
  datasource:
    url: jdbc:mysql://localhost:3306/merch?createDatabaseIfNotExist=true
    username: root
    password: 1qaz!QAZ


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  servlet:
    multipart:
      max-file-size: 16MB
      max-request-size: 16MB
      enabled: true
  doc:
    api-docs:
      path: /api-docs
    swagger-ui:
      path: /swagger-ui.html
      display-request-duration: true
      operations-sorter: alpha
      tags-sorter: alpha
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8080

# CORS: override in Docker with CORS_ALLOWED_ORIGINS (comma-separated)
cors:
  allowed-origins: "${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173,http://localhost:8081}"

storage:
  location: 'C:/Users/alexa/Documents/GitHub/Mech-App-Beckend/dms'

jwt:
  secret: ${JWT_SECRET:mech-app-secret-key-change-in-production-min-256-bits}
  access-validity-ms: 900000
  refresh-validity-ms: 604800000

api-key: xf0heYo7Cw7AeteadazPJwJTuPWSXDLx




