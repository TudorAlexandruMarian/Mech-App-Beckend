spring:
  application:
    name: eBanca
  datasource:
    url: jdbc:mysql://localhost:3306/merch?createDatabaseIfNotExist=true
    username: root
    password: 1qaz!QAZ


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  servlet:
    multipart:
      max-file-size: 16MB
      max-request-size: 16MB
      enabled: true
  doc:
    api-docs:
      path: /api-docs
    swagger-ui:
      path: /swagger-ui.html
      display-request-duration: true
      operations-sorter: alpha
      tags-sorter: alpha
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8080

storage:
  location: '/home/administrator/'




